# Отчёт по Task 6

Статус: **DONE**

## Результат

- Переписаны тексты ровно для 12 маршрутов: `/`, `/solutions`, `/features`, `/pricing`, `/integrations`, `/contractors`, `/developers`, `/enterprise`, `/about`, `/security`, `/contact`, `/blog`.
- У каждого маршрута отдельный вопрос и предметная лексика из brief.
- Для 12 маршрутов заданы уникальные `title` до 60 символов и `description` длиной 70–160 символов.
- `ProHelper` удалён из метаданных и пользовательских текстов. Технические пути `sourceOfTruth` и имена существующих файлов не переименовывались.
- Цены, состав пакетов, пробный период и правила продления не изменены. На странице цены сохранены 10 пакетов, полный комплект за 79 900 ₽, период 30 дней, пробный период 3 дня и льготный период 7 дней.
- Для `/security` оставлены только подтверждённые механизмы: роли и права, контекст организаций и объектов, привязка файлов к рабочим записям, защищённые правами журналы и истории для поддерживаемых операций.
- Фактический маршрут `/blog` использует `src/components/blog/public/BlogPublicPage.tsx`; обновлён именно этот источник. Устаревший `src/pages/resources/BlogPage.tsx` также очищен от выдуманных авторов, дат, кейса, суммы экономии и неподтверждённых результатов.

## TDD и контракт

До изменения production-текстов в `marketingContent.test.ts` добавлен контракт для ровно 12 маршрутов. Он проверяет:

- длину и уникальность метаданных;
- отсутствие старого бренда в метаданных;
- обязательную предметную лексику каждого маршрута;
- привязку проверки к фактическим источникам пользовательского текста.

RED подтверждён: контракт падал на старом бренде и прежних текстах. После реализации контракт стал GREEN.

## Humanizer-pass

Найдены и исправлены AI-паттерны:

- повторение слов «контур» и «сценарий» вместо предметных сущностей;
- абстрактные «релевантный блок», «управляемый результат», «текущий ландшафт»;
- одинаковые CTA «покажем / разберём» вне демонстрации;
- общие обещания прозрачности, скорости и результата без проверяемого механизма;
- нерасшифрованные API, ERP, BI, SSO, SLA, RFI, HSE, ПД/РД и BIM/IFC;
- выдуманные авторы, даты, кейс, сумма экономии и кратные результаты в старой странице блога.

После прохода тексты привязаны к объектам, задачам, ролям, документам, справочникам, объёмам, оплате и правам. `Покажем` и `разберём` оставлены только в CTA и описаниях демонстрации/контакта. Искусственная разговорность, юмор и неуместное первое лицо не добавлялись.

## Проверки

- `npx vitest run src/data/marketing/marketingContent.test.ts src/data/marketing/siteIndex.test.ts src/data/marketing/packages.test.ts src/data/marketing/publicCommercialModel.contract.test.ts` — PASS, 4 файла, 27 тестов.
- `npx tsc --noEmit` — PASS.
- `npx eslint <изменённые TS/TSX>` — PASS.
- `npx --yes prettier@3.8.3 --check <изменённые TS/TSX>` — PASS.
- `git diff --check` — PASS.
- Статический поиск старого бренда — в `common.ts` совпадений нет; в общем списке изменённых файлов остались только технические пути `sourceOfTruth`, проверки контракта и имена существующих assets.
- Статический поиск кейсов, отзывов, логотипов, гарантий, процентов и кратных результатов — новых пользовательских утверждений нет. Совпадения `за 30 дней` относятся к неизменяемой коммерческой модели.
- Build и dev-сервер не запускались по правилам проекта.

## Self-review

- Layout и API страниц не менялись; изменены data и пользовательские строки.
- Dashboard и личный кабинет не затронуты.
- `packages.ts` и коммерческие значения не изменялись.
- Уникальные intent и обязательная конкретика всех 12 маршрутов покрыты контрактом.
- Неподтверждённых security claims, кейсов, отзывов, логотипов, гарантированных результатов и выдуманных сроков не добавлено.

Concerns: нет.

## Review Fixes

- Контракт для 12 основных маршрутов теперь читает экспортируемые данные напрямую и связывает каждый маршрут с точным компонентом из `App.tsx`. Проверка пользовательского текста компонента выполняется через AST, без поиска произвольных подстрок по общим файлам.
- Добавлены отрицательные проверки для гарантированных эффектов, процентов, числовых сроков вне `/pricing`, старого бренда и гибридных названий, а также необъяснённых `ИД`, `IFC`, `punch-list`, `change orders`, `Доверительный слой` и `cookie-consent`.
- RED подтверждён двумя ожидаемыми падениями: на гарантированных эффектах и необъяснённой терминологии в общедоступном тексте, а также на статусе «В продукте» у подготовки обмена документами.
- Формулировки возможностей переведены из обещаний результата в проверяемые описания карточек, статусов, ответственных, реестров и отчётов. Числовые коммерческие сроки сохранены только на `/pricing`.
- Первые публичные упоминания терминов раскрыты: «исполнительная документация», «формат IFC для информационных моделей», «перечень замечаний при приёмке (punch-list)». `Change orders`, `Доверительный слой` и `cookie-consent` заменены понятными русскими формулировками.
- Карточка «Документы для обмена» получила статус «Этап настройки»; базовые файлы в МОСТ сохранили статус «В продукте».
- Humanizer-pass повторён после исправлений: удалены бездоказательные обещания скорости и потерь, канцелярские англицизмы и необъяснённые сокращения; тексты оставлены предметными и нейтральными.
- GREEN: `marketingContent.test.ts` — 14/14; полный целевой набор — 4 файла, 29/29 тестов.
- Финальные проверки: `npx tsc --noEmit`, ESLint изменённых TS/TSX, Prettier 3.8.3 и `git diff --check` — PASS.
