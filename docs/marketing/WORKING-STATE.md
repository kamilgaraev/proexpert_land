# МОСТ — прочитать первым после восстановления контекста

Обновлено: 2 сентября 2026 года, после локальной проверки четырёх основных страниц и повторной просьбы пользователя вести записи. Это рабочая точка восстановления, не отчёт о завершённом редизайне. При возобновлении сначала сравнить с Git и фактическим состоянием инструментов.

## 1. Последнее решение пользователя — приоритетное

**Утверждён светлый вариант** из `references/approved-home-20260902.png`. Пользователь сказал оставить именно его и распространить стиль на весь сайт. Шапка широкая, крупная чёрная типографика, сцена стройка/офис, ровное оранжевое полотно, опоры М. Не возвращаться к тёмным чертежам/тонким подвесным мостам: эти исследования отклонены, даже если в старой памяти есть рекомендации их развивать.

Самое главное — история и брендинг всего сайта, а не набор несвязанных блоков. Пользователь разрешил самостоятельную реализацию и крупные выпуски; направление теперь утверждено. Не просить повторного разрешения на уже согласованную работу. Генерация изображений допустима по необходимости, видео отложено.

Пользователь потребовал вести документацию, потому что после сжатия контекста решения теряются. Обновлять эту запись после каждого крупного изменения/выпуска и перед остановкой. Не считать устную сводку достаточной.

Протокол восстановления: прочитать этот файл → проверить Git status/HEAD и активную цель → прочитать соответствующую спецификацию → проверить существование указанных артефактов и состояние процессов → продолжить с первого незакрытого действия. Не повторять генерации, проверки или деплой по памяти. При противоречии старой записи с текущим Git или результатом инструмента сначала установить фактическое состояние и исправить запись.

В каждой точке остановки сохранять: последнее решение пользователя и отвергнутые варианты; изменённые файлы и назначение правок; выполненные проверки с результатом и артефактами; непроверенные риски; commit/ветку/деплой; один следующий конкретный шаг. История бренда и дизайн-система описывают правила, этот файл — состояние исполнения. Не смешивать обещание, реализацию, локальную проверку и публикацию.

## 2. Документы, которые нужно прочитать

1. `design-system.md` — бренд, эталон, сетка, типографика, компоненты, изображения, адаптивность.
2. `story-and-pages.md` — общая история, назначения семейств страниц, пользовательские истории, правила текста и доказательств.
3. `acceptance-and-audit.md` — исходные оценки, проблемы A01–A12, проверка и реальные границы готовности.
4. `../superpowers/plans/2026-09-02-marketing-bridge-redesign.md` — этапы и следующий выпуск.
5. Исходный полный аудит: `C:/Users/kamilgaraev/.codex/audits/most-2026-09-02/report.md`; приложения в той же папке.

Слова «10/10» не заменяют доказательства. Исходная общая оценка 4,5/10, дизайн-система 4/10, мобильный UX 4/10, доверие 3/10. Новые баллы ещё не выставлены.

## 3. Git и публикации

- Каталог: `C:/Users/kamilgaraev/Desktop/prohelper_full/prohelper_land`.
- Ветка: `feat/marketing-bridge-redesign-20260902`.
- Последний программный commit перед этой записью: `5acc82ba68b9521ee52158c2036b6411a5dfbe59`.
- PR #18: https://github.com/kamilgaraev/proexpert_land/pull/18, общая ветка последовательных выпусков.
- Последний известный production-выпуск: run 33662625635, SHA выше. В этой итерации нового деплоя не было.
- Первая версия технически работала, но пользователь справедливо отклонил качество hero/шапки, тяжёлую рамку скриншота и артефакт человека в столе. Не считать её визуально принятой.
- `AGENTS.md` изменён другой работой: не включать в коммиты и не откатывать.
- `.playwright-cli/` — локальные артефакты браузерной проверки, не включать в продуктовый commit.
- Некоторые пути docs игнорируются Git. Добавлять только явные документы этой задачи через `git add -f`, не всю папку или все игнорируемые файлы.

Резервная копия создана и проверена ранее: `/var/backups/most-marketing/before-redesign-20260902`, run 33657612577, SHA-256 `8f7bf752c5e37d8b2891de545f0a54907a5150287222425eeacd685830eb8cd0`. Git bundle и инструкция: `C:/Users/kamilgaraev/.codex/audits/most-2026-09-02/redesign-backup/`. Не перезаписывать архив. Деплой — штатный workflow, цель `marketing`. Push в main публикует также кабинет, поэтому промежуточные выпуски идут вручную с task-ветки. SSH только read-only.

## 4. Подготовленные локальные изменения

Все перечисленные ниже изменения подготовлены и staged, но ещё не выпущены. Документы концепции сохранены отдельным commit `0181bafe`; это не commit нового исполняемого кода.

- `src/components/landing/MarketingShell.tsx`: новые CSS системы.
- `src/components/marketing/MarketingPrimitives.tsx`, `blocks/CtaBand.tsx`: плоские общие заголовки/hero/CTA.
- `src/pages/landing/FeaturesPage.tsx`, `SolutionsPage.tsx`, `PricingPage.tsx`: новые продукт, решения и выбор стоимости.
- `src/data/marketing/capabilities.ts`: предметные публичные формулировки без изменения идентификаторов/цен.
- `src/styles/marketing-pages.css`, `marketing-pricing.css`.
- `PricingPage.test.tsx`, `HomePage.seo.test.tsx`.

Последние изменения в том же подготовленном блоке:

- `HomePage.tsx`: широкая светлая композиция; подключён исправленный `most-bridge-v2-*`; старый Гант с тяжёлой рамкой удалён; вместо него честный HTML-пример заявки. Повторный общий блок из трёх шагов удалён: после hero сразу один предметный рассказ «Одна заявка. Общая работа.». Якорь `#workflow` теперь на нём; `#product` сохранён внутри. Затем роли, начало работы/цена, FAQ, контакт.
- `marketing.css`: адаптация header/hero, подписи на мосте; пример заявки; нейтральный светлый фон и графитовая стартовая панель. Точки на мосте проверены. Для невысокого desktop добавлена композиция, при которой в 1366×768 видны ноги М. Контактная форма принудительно видима без декоративного fade, вложенная плашка согласия упрощена. Последняя правка плашки ещё требует минимальной проверки CSS.
- `CookieBanner.tsx`: убраны щит, тяжёлая тень/размытость и большие скругления; компактный плоский блок с равноправными кнопками. Логика get/saveCookieConsent и подписка на событие сохранены. В браузере проверены отказ от аналитики, сохранение essential=true/analytics=false и отсутствие повторного баннера после перезагрузки.
- `MarketingPrimitives.test.tsx`: новый тест общей навигации/заголовков/ссылок прошёл; форматирование завершено.

## 5. Изображения

Исправленная сцена создана встроенным imagegen: `C:/Users/kamilgaraev/.codex/generated_images/01a062ae-b363-7c80-b5b5-1feb38cb4f69/exec-4e4f0c51-d918-436a-92d7-35bf69ee7ced.png`, 1774×887. Задача редактирования: сохранить всю сцену, исправить посадку человека за столом и физические пересечения в офисе. Проверены полное изображение и увеличенный офис. Исходник не удалён.

Подготовлены и подключены в workspace:

- `public/images/marketing/most-bridge-v2-640.webp` — 23 786 байт.
- `public/images/marketing/most-bridge-v2-1024.webp` — 58 204 байта.
- `public/images/marketing/most-bridge-v2-1774.webp` — 147 614 байт.

Старые версии оставлены для безопасного отката и истории. Новый исходник ещё не опубликован. Эталон пользователя скопирован в docs/references как файл, указанный в начале этой записи.

## 6. Проверки — что доказано и что нет

Ранее в этой задаче: 32 теста общего маркетинга + 2 теста цены + 1 тест MarketingPrimitives прошли. ESLint/Prettier прежнего законченного набора проходили. Общая проверка типов имеет три исходные ошибки Axios в неизменённом `src/utils/api.ts`. Не объявлять full typecheck зелёным.

Локальная проверка главной на 360, 390, 768, 1080, 1366, 1440, 2215 px: переполнения по ширине и отсутствующих якорей не обнаружено. Затем отдельно проверены главная, продукт, решения и цена на 390/1440 px после обновления cookie-баннера: на каждой один H1, ширина документа равна viewport, ошибок pageerror/console не получено. Мобильная главная — 5925 px, desktop — 4734 px. Это локальные наблюдения, не production и не новый Lighthouse.

Просмотрены `home-final-1366.png` (вся М помещается), `home-final-360.png`, `request-final-1366.png`, `contact-final-360.png` и полные страницы из `output/playwright/approved-light/`. Последняя правка снятия фона плашки согласия сделана после снимка контакта. Не приписывать снимку проверку этой правки.

Последний целевой прогон HomePage.seo.test.tsx: 1 тест прошёл. ESLint HomePage/CookieBanner/MarketingPrimitives.test и Prettier этих файлов прошли. Prettier трёх CSS прошёл до последней небольшой правки плашки согласия. git diff --cached --check прошёл после включения всех 18 файлов в staging.

В ранней съёмке был hydration mismatch по внедрённому screenshot-инструментом `caret-color:transparent`. При ожидании клиентской готовности и `caret: 'initial'` свежий обход не дал ошибок. Белый skip-link на длинном снимке контакта также оказался артефактом съёмки: в реальном viewport ссылка вне экрана и не в фокусе. Не исправлять эти симптомы изменением приложения без воспроизведения.

Gortex review staged: BLOCK, 0 конкретных findings. Основание — большой fan-out общих primitives и графически не найденное покрытие. Это не подтверждённый баг и не положительное ревью. Часть blast radius вызвана форматированием неизменной логики. Перед выпуском сопоставить реальные изменённые контракты с прошедшими тестами и браузерными сценариями, зафиксировать решение; не повторять полное ревью только ради зелёной отметки.

Документы не требуют автотестов. Для них проверить ссылки, UTF-8, соответствие Git и границы утверждений.

## 7. Живые инструменты — перепроверить перед использованием

- Dev-server слушал 127.0.0.1:5173, PID 19912. Не запускать второй, пока не проверен порт/процесс. Локальный dev нужен для конкретной визуальной отладки; production build локально запрещён.
- CUA: tab 4 — локальная главная, tab 3 — production. Встроенная панель обрезает desktop-снимок при увеличенном виртуальном viewport.
- Playwright CLI: именованная сессия `most-light`, была открыта с PID 27148. Команда `npx.cmd --yes --package @playwright/cli playwright-cli -s=most-light ...`. Проверить session list, если возобновляется после перерыва.
- Скрипты обходов в `C:/Users/kamilgaraev/.codex/audits/most-2026-09-02/`: `approved-light-qa.js` (7 ширин), `approved-light-current-qa.js` (4 страницы, cookie, консоль), `approved-light-final-qa.js` (низкая desktop-композиция и мобильный контакт). Артефакты: `output/playwright/approved-light/`.
- В этой точке нет ожидаемого job генерации, сборки или деплоя. Не путать старые handles с живыми процессами.

## 8. Проверка перед выпуском блока 2

- Последняя правка согласия: Prettier marketing.css прошёл, браузер на 390 px подтвердил прозрачный фон, radius/padding=0 и отсутствие переполнения. Снимок `output/playwright/approved-light/consent-final-390.png` просмотрен. Новые сообщения консоли — предупреждения development React Router, не ошибки приложения.
- Сопоставлен реальный staged diff MarketingPrimitives с тестом: изменения поведения ограничены HTML/классами SectionHeader, PageSectionNav, PageHero. Параметры и адреса действий сохраняются; MarketingLink по-прежнему разделяет обычные ссылки/hash и Router. Форматирование LegalDocumentView, formatPackagePrice, значков и статусов не меняет алгоритмы. Реальный тест проверяет иерархию H1/H2, nav, hash, mailto и переход Router. Другие страницы дополнительно покрыты предыдущими 32 тестами и браузерным обходом.
- Решение по машинному review: разрешить ограниченный выпуск проверенного блока с сохранением записи BLOCK/0 findings как эвристического сигнала fan-out. Это инженерная оценка фактического diff и доказательств, а не утверждение о зелёном автоматическом ревью или готовности всего сайта. Остальные семейства страниц остаются в плане; не объявлять их визуально принятыми.
- Workflow проверен: файл `.github/workflows/deploy.yml`, input `target=marketing`, ref текущей task-ветки. Последний production run всё ещё 33662625635, завершён успешно; нового активного выпуска до отправки команды нет.

## 9. Следующие конкретные действия

1. Проверить форматирование последней правки marketing.css и её отображение в контакте. Основные lint/тесты/браузерный обход уже выполнены; не повторять весь набор из-за документации.
2. Закрыть оценку риска общих primitives по реальному diff и уже имеющимся тестам; честно сохранить машинный BLOCK и принятое по доказательствам решение. Gortex guards: правил не настроено. Не выдавать это за прохождение содержательной проверки guards.
3. Завершить логический блок и commit только собственных исходников/изображений/документов. Не включать AGENTS.md и .playwright-cli. Восстановить точное имя workflow input из .github/workflows/deploy.yml; не угадывать.
4. Push task-ветки, manual deploy только marketing. Дождаться CI и выполнить production QA четырёх страниц и новых изображений. Записать SHA/run/результаты; до этого состояние — локально подготовлено.
5. Продолжить все остальные семейства: SeoClusterPage, три приоритетные роли, остальные коммерческие страницы, блог/категории/статьи, компания/контакты/безопасность/интеграции/legal. Привести к системе, не ограничиваться общей шапкой.
6. Закрыть A01–A12 исходного аудита, измерить фактический результат, обновить оценки только с доказательствами. Цель остаётся активной до готовности всего объёма.
