# Проверка общих зависимостей и типов — 4 сентября 2026

Статус: локально проверено, ещё не выпущено. React и react-dom оставлены 19.1.0 по последнему указанию пользователя; типы React 19.1.2/19.1.3. Версии закреплены точно, чтобы установка не повторила отменённое обновление.

Обновлены Axios 1.9.0 → 1.20.0, React Router 6.30.1 → 6.30.6. Через Axios обновились form-data 4.0.2 → 4.0.6 и follow-redirects. Удалён старый пакет типов Axios 0.9; клиент использует встроенные типы Axios 1.x. Поведение авторизации и повторных запросов не изменено.

Исправлены накопленные ошибки TypeScript: отсутствующая дата не передаётся в dateTime, неиспользуемый eyebrow заголовка необязателен, подключены типы Vite для env/assets, контекст SSR импортируется из правильного модуля и дополнен фактическими полями приложения, mediaType в тестовой фикстуре использует null.

Проверки:
- npm ls подтверждает React/react-dom 19.1.0 и прежние версии типов.
- npx tsc --noEmit: PASS, session4563 exit0.
- ESLint восьми затронутых TS/TSX/declaration файлов: PASS.
- Vitest api, blogPublicApi, marketingAssetFilter, MarketingPrimitives, SeoClusterPage: 5 файлов, 60 тестов PASS, 8.35s.
- Gortex: impact/context, detect_changes, test_targets, guards выполнены. Guards не настроены. Staged review: REVIEW из-за средней области влияния, конкретных findings 0; это проверка правил, не независимое визуальное ревью.

Аудит до блока: 29 пакетов, critical1/high18/moderate6/low4. Промежуточный аудит после обновления ядра: 25 пакетов, critical0/high14/moderate7/low4. Он начат до возврата React, поэтому не считать его окончательным аудитом текущего lock. Остаток безопасности (Vite/esbuild/SSR plugin и другие цепочки) ещё не закрыт. Не применять предложенный audit downgrade vite-plugin-ssr до0.4.85 вслепую.

В Git отслеживается часть node_modules; установка изменила эти локальные файлы. Они сознательно не включены в staged-блок: источником установки должны быть package.json и lock. Изменение политики отслеживания зависимостей в этот блок не входит.

Выпускать маркетинг отдельно. Перед редизайном ЛК нужен отдельный проверенный бэкап после завершения сайта. Черновик workspace.css не подключён и не входит в этот выпуск.
