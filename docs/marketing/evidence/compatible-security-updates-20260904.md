# Совместимые обновления зависимостей — 4 сентября 2026

Обновлены выбранные зависимости в пределах существующих semver-диапазонов штатным npm update. package.json не изменён; меняется только package-lock.json. React и react-dom остаются 19.1.0, основной Vite остаётся версии 5. Major-миграции и принудительный audit fix не выполнялись.

Обновлены Unhead 2.1.17, PostCSS 8.5.28, Happy DOM 20.14.0 и исправленные совместимые версии Babel, Rollup, glob, minimatch, brace-expansion, picomatch, nanoid, YAML, Socket.IO parser/Engine.IO client и ws. Context7 npm/cli подтвердил семантику выборочного npm update; предварительный dry-run просмотрен.

Проверки: tsc --noEmit успешно; четыре файла Vitest (SEO, главная SEO, блог, категории) — 66 тестов успешно за 8.19 с. Gortex detect_changes: только package-lock.json; review APPROVE, 0 замечаний. React/react-dom и прямые обновлённые версии подтверждены npm ls. Сборка локально не запускалась согласно правилам проекта; проверка сборки — в штатном CI.

До обновления текущий audit показывал 25 пакетов: 0 critical, 14 high, 7 moderate, 4 low. Повторный audit ещё выполняется; итог не заявлен. Изменения пока не опубликованы. Установщик сообщил невозможность удалить старый занятый Windows-бинарник Rollup во временном каталоге node_modules; установка завершилась успешно, проверки прошли. Отслеживаемые файлы node_modules не включаются в коммит.
