# МОСТ — уточнение фокуса ползунка, 3 сентября 2026

## Причина

Пользователь не принял оранжевую прямоугольную обводку. Предыдущий выпуск убрал фокус области захвата, но оставил outline на ::before. Теперь у псевдоэлемента нет обводки: обычная полоска 8px/#85847f, клавиатурный фокус 12px/--most-ink с радиусом 6px. Область захвата остаётся прозрачной. Обработчики клавиатуры и перетаскивания не менялись.

## Проверка

Gortex доступен: index_health health=100, failures=0. Impact CSS LOW (граф не доказывает полноту CSS-связей). get_test_targets не нашёл тестов CSS, guard rules отсутствуют.

Собственная скрытая IAB tab25, localhost:5173, 1440×960. Переход Tab с последней ссылки приводит фокус на role=scrollbar; :focus-visible true, outline none у элемента и ::before, ползунок 12px, графитовый, фон rgba(0,0,0,0). Скриншот осмотрен: рамки нет. PageDown прокручивает, Home возвращает к началу. После Tab фокус снят, ширина снова 8px. Viewport сброшен. Пользовательские вкладки не трогали.

Git diff --check PASS. Автотесты ради CSS не добавлялись: регрессия проверена в реальном браузере. Production 9187bb6f опубликован: workflow33783333523 SUCCESS за1м17с. Своя скрытая IAB25,1440×960: клавиатурный focus-visible true, outline none у элемента и ::before, ширина12px/радиус6px, фон rgba(0,0,0,0). Скриншот осмотрен, прямоугольной рамки нет. Viewport сброшен.

## Отдельный незавершённый блок

Три сцены CRM, ERP и сверки 1С и их тексты остаются незакоммиченными. ESLint трёх TS/TSX PASS. Прежний процесс 63586 больше недоступен, поэтому восстановлен целевой прогон: 74 PASS / 4 FAIL. Лог output/office-scenes-tests.log. Два падения marketingContent требуют CRM/ERP буквально в H1; два commercialBoundary касаются ранее изменённого foreman: точной старой фразы ограничения и ошибочного ограничения видимого описания до 160 знаков. Не выдавать модуль за зелёный и не менять тесты механически: сверить реальные метаданные и смысловые ограничения.

Новые изображения (осмотрены локально 1440/390 предыдущим ходом): most-client-project-branded-v1, most-resource-planning-branded-v1, most-accounting-reconciliation-branded-v1; каждое WebP1536/720. Оригиналы output/imagegen/most-scenes-20260903. На папках настоящий знак МОСТ, без летящих бумаг. Эти файлы не входят в выпуск фокуса. Общая цель ACTIVE.
