# МОСТ — проверка контентных контрактов, 3 сентября 2026

## Изменение

Возвращено отображение processComparison.note в общем шаблоне профильных страниц. Данные содержали существенные ограничения, но шаблон их не выводил. Пояснение использует общий размер most-type-small и приглушённый цвет; проверено на широком и мобильном экранах.

Из SEO, карточек решений и OG ПИР удалены неподтверждённые обещания BIM/IFC. Теперь описаны версии проектной и рабочей документации, замечания и выпуск комплектов. PNG обновлён из SVG; переносы согласованы с генератором. Фиолетовое оформление старого OG остаётся отдельным долгом общего бренд-аудита.

Первое употребление RFI и punch-list расшифровано в видимых текстах. Проверки терминов перенесены с механической конкатенации данных на фактически отрисованную страницу. Проверяются также первое употребление HSE/ЭП и юридическое условие FAQ документов. Устаревшие проверки дословного совпадения H1 с поисковой фразой заменены проверками редакционных заголовков; точное основное намерение поиска по-прежнему проверяется в метаданных. Enterprise проверяется по фактически используемым данным, без принудительного multi-org из неиспользуемого реестра.

## Проверки до выпуска

- ESLint изменённых TS/TSX и генератора: PASS.
- Prettier изменённых TSX/тестов: PASS.
- Полный целевой прогон marketingContent.test.ts и SeoClusterPage.test.tsx: 73 PASS, 1 FAIL. Оставшаяся ошибка ожидала слово «внешний» в корректном описании оплаты через банковскую систему. После исправления смыслового ожидания повторён только этот тест: 1 PASS. Итого все 74 проверки пройдены в полном и целевом прогонах; отдельного полного зелёного повтора не выполняли.
- 29 проверок рендера подтверждают вывод настроенных пояснений.
- Gortex staged review: 0 findings; REVIEW из-за MEDIUM области влияния общего компонента и отсутствия тестовых связей языка в индексе. Фактические целевые тесты выполнены.
- git diff --cached --check: PASS.
- Локальная собственная скрытая IAB: оценка сметы 1440×960 и мобильное приложение 390×844, пояснения видны, переполнения нет. OG PNG осмотрен, текст помещается.

Локальную сборку не запускали. Чужие AGENTS.md и неотслеживаемые файлы не входят в выпуск. ## Результат выпуска

Commit 054484796467f71fc074678f2fd918ebf724131d опубликован штатным workflow 33775771558, conclusion success. Первая попытка gh run view получила TLS handshake timeout; повторно прочитан тот же run, новый выпуск не запускался.

Production проверен в собственной скрытой IAB tab22. ПИР: description без BIM/IFC, настроенный OG URL, видимое пояснение об инженерной проверке, горизонтального переполнения нет. Мобильное приложение при 390×844: пояснение о роли и качестве связи читается под фактическим сценарием, переполнения нет. Оба блока визуально осмотрены; viewport сброшен. OG PNG отвечает HTTP 200, image/png, 78606 байт, совпадает с новым экспортом. Чужие вкладки не использовались. Общая цель остаётся ACTIVE.
