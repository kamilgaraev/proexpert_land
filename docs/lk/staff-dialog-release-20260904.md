# Форма сотрудника — 4 сентября 2026

## Исправление
На мобильном диалог перекрывался шапкой и обрезался по ширине. Корневой слой диалога поднят выше оболочки, панель получила полную доступную ширину с ограничением 2xl, внутренние отступы 16/24 px. Убраны градиенты основных секций и кнопок, заголовок уменьшен, поля используют текст 16 px. Кнопки раскрытия пароля имеют доступные имена и ширину 44 px. Длинная подпись активности переносится. Системные роли больше не оформлены капсом.

Создание, обновление, назначение ролей, валидация и отправка API не изменялись. Производственные записи для проверки не создавались.

## Проверки
- ESLint изменённого файла: успешно.
- TypeScript --noEmit: успешно.
- AdminFormModal.test.tsx: 1 тест успешно.
- Gortex: замечаний правил нет; средний радиус влияния, граф не связывает тест с компонентом.
- Deploy 33826289202 успешно завершён. В собственной скрытой вкладке IAB 31 при 390×844: диалог над шапкой (z-index 70), ширина документа 390 px, поля и нижние кнопки помещаются. Кнопка «Показать пароль» меняет тип поля на text; «Отмена» закрывает форму. Ничего не отправлялось и не сохранялось. Скриншоты проверены после окончания перехода. Список ролей остаётся длинным; его компактность — последующее улучшение, а не дефект перекрытия.

## Предыдущий выпуск
Диаграммы a761ee3a: deploy 33825771113 завершён успешно; геометрия в браузере ещё требует проверки.
