# Обзор и общие элементы ЛК

## Наблюдения production

Собственная IAB25,1280×720: первый переход на Главную перенаправил в проекты, второй открыл обзор. Это существующая одноразовая workspace-redirect логика DashboardLayout, не сломанный Link. Логику не меняли. В обзоре: оранжевый градиент финансовой карточки, маленькие иконки, обрезанные быстрые действия. Общие количества total ошибочно подписаны как активные проекты и подписанные договоры. Время «Обновлено» вычислялось при отрисовке, а не при получении данных.

## Изменения

Обзор: нейтральные карточки вместо градиента и декора; крупнее иконки и подписи действий; больше места карточкам на промежуточных экранах; перенос текста действий. Подписи total приведены к общим количествам, псевдовремя обновления убрано. Запрос данных, права и графики не менялись. Убрано слово «дашборд» в сообщениях, «партнёры и бонусы» заменено фактическим назначением ссылки — приглашениями подрядчиков.

Общий Sheet: русское доступное имя закрытия, SVG20px, область44×44px и фокус только для клавиатуры. Глобальный scrollbar: нейтральный thumb и прозрачный track, без градиента. Это не превращает нативную полосу браузера в overlay: отдельная проверка внешнего вида обязательна, обещания полного исчезновения занимаемого места нет.

## Проверки

ESLint DashboardPage и Sheet PASS. DashboardPage тест загрузки и локализации статусов PASS; тест ошибки первоначально ожидал старый текст, обновлено ожидание, целевой повтор PASS. Sidebar2теста PASS. Повторный tsc не запускался: изменения этого блока ограничены JSX/CSS и текстами, последний полный tsc прошёл на11ad26a7. Gortex detect staged:3файла; guards отсутствуют; review REVIEW,0findings, средний риск. Изменение ожидания теста не меняет исполняемый контракт.

Commit f56ccfca, запущен target=lk workflow33824095617: https://github.com/kamilgaraev/proexpert_land/actions/runs/33824095617. Выпуск завершился SUCCESS. Собственная свежая IAB27: нейтральный scrollbar появился; мобильное меню имеет кнопку «Закрыть», её нажатие закрывает меню. Обзор390×844 показал горизонтальное переполнение: auto-колонки графиков сохраняли intrinsic-ширину canvas533/946px. Это дефект приёмки; обзор пока не принят. Исправление: явные grid-cols-1 у базовых сеток и min-w-0 на карточках графиков, без скрытия overflow. ESLint PASS; функциональные тесты не повторялись, так как менялись только классы. Повторная браузерная проверка после следующего выпуска обязательна. Новая визуальная приёмка этого блока после CI ещё обязательна. Собственная вкладка25 уже содержит прежнюю версию; после выпуска открыть свежую скрытую IAB. Перейти на Главную дважды, если сработает штатный первый redirect. Проверить обзор1280/390/768, scrollbar без градиента, мобильное меню и кнопку «Закрыть». Пользовательские вкладки2/4 не трогать. Последний viewport reset выполнен. Следующие общие задачи: убрать служебные описания workspace из sidebar, формат баланса, единая типографика всех уровней, формы/таблицы/порталы, остальные экраны ЛК. Полная готовность не заявляется.
