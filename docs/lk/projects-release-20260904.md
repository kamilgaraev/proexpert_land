# Экран проектов — 4 сентября 2026

## Выполнено

Страница использует общий отступ оболочки без второго узкого контейнера. Убраны декоративный градиент карточки, тени и сдвиги кнопок, вложенные рамки показателей. SVG20px; заголовки карточек используют общий размер раздела. Колонки:1 до1024px,2 до1536px,3 выше. Заголовки и адреса не обрезаются. Общие суммы, роли, права, маршруты и разделение своих/чужих проектов сохранены. Подпись общего количества работ теперь «Работы», поскольку total не доказывает завершение.

Переход выполняет доступная кнопка с именем проекта; карточка не имитирует кнопку недоступным кликом по div. Готовность имеет progressbar с числовым значением. Поиск получил доступную подпись. Некорректный ответ или ошибка сети показывают отдельное состояние с повторной загрузкой; закрытый экран не обновляет state после ответа.

## Проверки

Vitest/MSW2теста PASS:503 не подменяется пустым списком, повторная загрузка восстанавливает вложенный projects-ответ; плоский массив поддерживается, поиск фильтрует список. ESLint3файла PASS. TypeScript PASS. Gortex detect завершён; guards не настроены. Rule review0замечаний, но BLOCK из-за графовой оценки HIGH/uncovered для страницы. Это ограничение связи графа с тестами: фактический тест монтирует MyProjectsPage, вызывает её загрузку через MSW, проверяет ошибку, retry и поиск. Не утверждаем, что графовое ревью одобрило изменение.

Локальная сборка не запускалась по правилам проекта. Авторизованный локальный просмотр ограничен production CORS; визуальная проверка нового экрана обязательна после обратимого CI-выпуска на домен ЛК, до признания блока готовым. Commit11ad26a7 отправлен, запущен LK workflow33823362776: https://github.com/kamilgaraev/proexpert_land/actions/runs/33823362776. Выпуск SUCCESS. Собственная скрытая IAB25: проверены1280×720,390×844 и768×1024; переполнения на390 нет. Поиск без совпадений показывает пустой результат, поиск «Тестовый» возвращает карточку. Данные9контрактов/282215₽ и1работа/4763₽ сохранены. Размер окна восстановлен. Вызов fill с пустой строкой в браузерном инструменте только выделил прежний текст; успешную очистку в production этим действием не заявляем. Сам поиск с новым значением проверен. Не запускать этот workflow повторно. Предыдущая оболочка c48fb559 опубликована и проверена на1280/390.
