# Поддержка и безопасность: контрольная точка

Предыдущий ход — progress: исправлена ложная успешная отправка, согласована почта аккаунта, обновлён sharp с подтверждённым устранением high advisory. Общая цель активна. Весь ЛК ещё не завершён.

HEAD a5f252ec отправлен в origin, ветка feat/marketing-bridge-redesign-20260902, PR18. Чужие AGENTS и tracked node_modules не менять и не добавлять в коммиты. Для sharp npm обновил ещё три установленных пакета; в коммит включены только package.json и lockfile.

## Выпуски

- 33829924389 / 2e106f25 — SUCCESS.
- 33831028363 / 8a2139db — SUCCESS. Включает FAQ и исправления поддержки. Форма проверена в собственной IAB40 на ширине390: поля16px/44px, имя и почта аккаунта readOnly, textarea.maxLength5000, scrollWidth382. Письма не отправлялись.
- 33831283769 / a5f252ec, target=both — последняя проверка IN PROGRESS. Это обновление sharp. Дождаться того же CI и проверить маркетинг/ЛК после общего выпуска; не запускать дубликат.

## Проверки

Поддержка: 8 тестов Vitest, ESLint и tsc --noEmit прошли. До исправления ответ500 показывал успех. Теперь проверяются HTTP2xx и success!==false. При отказе черновик сохраняется; технические исключения не отображаются. Состояния размечены role=alert/status. Backend использует авторизованного отправителя, поэтому форма показывает его имя/почту без редактирования. Тесты подменяют сервис, внешних писем нет. Подробности: support-submission-20260904.md.

Sharp: Context7 /lovell/sharp и npm view подтверждают Node>=20.9. Локально22.22.2, workflow использует22. Проверка в памяти PNG1200x630 → WebP600x315 прошла (sharp0.35.4/libvips8.18.6). Audit48335 завершён: осталось5 пакетов,4moderate/1high/0critical; sharp отсутствует. Не повторять аудит того же lockfile.

Остались esbuild, react-router, react-router-dom, vite, vite-plugin-ssr. Context7 доступен через functions: tools.mcp__context7__resolve_library_id/query_docs. Vite: /vitejs/vite. Современная ветка требует Node20.19+/22.12+. Router исправлен в >=7.18; автоматическое предложение downgrade SSR до0.4.85 не применять. React19.1.0 оставляем. Нужен отдельный согласованный блок совместимости Vite/SSR/esbuild/Router.

## Браузер

Использовать только собственные скрытые IAB. Пользовательские2/4 не трогать. own39=supportQA (старый выпуск); own40=latestHelpQA (актуальная форма, список типов открыт); own37=formsQA (billing); own38=helpQA (FAQ). ownViewport поддерживает set/reset, сейчас390x844. Первый кадр сразу после resize может быть переходным: перепроверять после снимка DOM. Секреты только в истории, не сохранять в файлы.

## Следующий блок

Выпадающий список типа обращения функционирует, но портал использует старый голубой цвет и тесные пункты. Исправить общую систему портальных компонентов: select, dialog, dropdown — размеры, цвет, типографика и фокус. Не клонировать стили отдельно на каждом экране. Затем оставшиеся экраны подрядчиков, профиля, инструкций, журнала изменений и блок безопасности. Маркетинг принят для перехода с оговорками, бэкап ЛК проверен; это не означает готовность всего ЛК.
