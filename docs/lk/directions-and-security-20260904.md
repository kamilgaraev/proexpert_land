# Направления работы и аудит зависимостей

4 сентября 2026.

## Направления
В собственной IAB35 в настройках компании обнаружены эмодзи и кликабельные div выбора режима. BusinessTypeSelector переведён на fieldset с именованными radio: поддержка клавиатуры средствами браузера, disabled у группы, доступные названия. SVG Briefcase заменяет эмодзи в селекторе и текущем режиме. Выбранная карточка не масштабируется и не получает тень. API и сохранение настроек не изменены. Два теста выбора/disabled и ESLint успешны, Gortex без замечаний правил. Клавиатурный сценарий в production ещё проверить, тест подтверждает семантику и callback.

## Безопасность: НЕ закрыто
npm audit --package-lock-only --json (сессия 22575) успешно вернул auditReportVersion 2: всего 6, moderate 4, high 2, critical 0. Выход 1 означает найденные уязвимости. Пакеты не изменялись. Вывод инструмента ограничен, перед планом миграции нужен полный компактный перечень цепочек.

Видны esbuild <=0.24.2 (GHSA-67mh-4wv8-2f99), React Router <7.18.0 (GHSA-wrjc-x8rr-h8h6), уязвимости Vite <=6.4.2, включая Windows fs.deny bypass GHSA-fx2h-pf6j-xcff, и цепочка vite-plugin-ssr → esbuild. Не применять audit fix --force: рекомендации включают major и даже понижение SSR-плагина. Проверить применимость к production и безопасную поддерживаемую миграцию с SSR/LK проверками. React 19.1.0 оставлен согласно указанию пользователя.

## Выпуск
Исправление отмены реквизитов ce05f786 / 33828008389 завершено успешно. Проверка формы опубликованной версии ещё предстоит. e800d309 с размерами полей и шкалой текста пока локальный commit, войдёт в следующий выпуск вместе с направлениями.
