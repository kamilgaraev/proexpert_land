# Общая навигация — уточнение

Убраны служебные description/modeLabels из Sidebar, оставлено название направления организации. Приоритеты и доступность маршрутов не изменены. Шапка принимает существующий OrganizationBalance вместо any; рублёвый баланс отображается через balance_cents/100 и Intl ru-RU, остальные валюты сохраняют серверное отображение. Иконки20px, очистка поиска44×44px, убрана двойная focus-ring вокруг поля.

Проверки: tsc --noEmit PASS, ESLint Header/Sidebar PASS. Gortex detect staged2файла, guards нет, review REVIEW0findings/средний риск. Автотесты маршрутизации повторно не запускались: обработчики и контракты поиска/меню не менялись, предыдущие sidebar-тесты PASS. Визуальная проверка после выпуска ещё требуется.

Предыдущий выпуск обзора f56ccfca workflow33824095617 на последнем опросе in_progress. Не запускать повторно. После его завершения проверить обзор и закрытие меню в новой собственной скрытой IAB, затем включить текущие мелкие правки в следующий блок. Не выдавать локальные изменения за опубликованные.
