# Диаграммы и следующий этап

Donut: отдельный относительный контейнер160×160px по центру, canvas-легенда выключена, значения/проценты вынесены в доступный HTML-список с прокруткой при большом числе статусов. Цвета согласованы с МОСТ. Положительные конечные значения образуют распределение; нулевое распределение имеет сообщение. Line: пустой ряд и полностью нулевой ряд имеют разные сообщения; отрицательные ненулевые балансы остаются графиком. Прямые отрезки вместо сглаживания, чтобы не создавать промежуточных вымышленных значений. Анимация отключена, canvas получил название.

Context7 Chart.js consulted: отдельный relative контейнер для canvas, maintainAspectRatio=false, выключение legend для HTML-подписей. Новые зависимости не ставились.

Проверки:4Vitest PASS (empty/zero, negative, legend values, zero donut), tsc PASS, ESLint PASS. Первый запуск нашёл пропущенную JSX-скобку; исправлена, повторные целевые проверки успешны. Gortex detect/targets/guards/review: APPROVE0findings. Автотесты подменяют renderer, поэтому геометрию canvas они не доказывают; после CI нужна реальная проверка.

## Предыдущий выпуск команды

33825233313 SUCCESS. Собственная свежая IAB29 /dashboard/admins390px: ширина382<390, список помещается. Создание открыто без ввода/сохранения и закрыто кнопкой Отмена. Никаких писем или мутаций. Найдены недостатки следующего блока: горизонтальная лента вкладок с тяжёлым scrollbar; AdminFormModal на мобильном экране имеет огромные отступы, тени/градиенты, заголовок обрезается, sticky header перекрывает верх диалога. Нужны z-index/portal, нормальные размеры и поля; права и назначение ролей сохранить. В модалке кнопки показа пароля без доступного имени. Файл src/components/dashboard/admins/AdminFormModal.tsx. Старые вкладки24-28 уже недоступны (таб28вернулnot part of session); не обращаться к ним. Вкладка29 — собственная. Viewport reset выполнен.
