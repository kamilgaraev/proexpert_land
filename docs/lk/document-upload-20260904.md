# Загрузка документов подрядчика — 4 сентября 2026

## Исправление

Родительская страница перехватывала ошибку API и завершала callback успешно. Форма после этого очищала название и выбранный файл. Теперь ошибка доходит до формы; форма показывает понятное сообщение и сохраняет данные для повторной попытки. После успеха очищаются и состояние, и сам файловый input. Во время запроса поля документа недоступны для изменения.

Права, формат запроса и серверное хранение не менялись. Реальные файлы на production не загружались.

## Проверки

- ProfileEditor.test.tsx: 3 теста прошли, включая отказ, сохранение файла и названия, повторную попытку и очистку после успеха.
- ESLint трёх изменённых файлов, TypeScript noEmit и git diff --check прошли.
- Gortex: нарушений правил не найдено; риск MEDIUM. Родительский обработчик не покрыт отдельным интеграционным тестом; его передача ошибки проверена чтением реализации. Тест формы проверяет отклонённый callback.

## Приёмка предыдущего выпуска

CI 33835055146 успешно выпустил 6fcf598b. В собственной скрытой IAB-вкладке 48 проверены профиль подрядчика и поля на 390×844, 768×1024 и 1440×1000. Горизонтального переполнения нет: scrollWidth 382/760/1432 соответственно. На планшете повторяющиеся категории и документы имеют широкие поля, список доступности открывается поверх формы и закрывается Escape. Название компании только фокусировалось, данные не менялись, сохранение и публикация не выполнялись.

## Остаток

После следующего выпуска проверить новую форму документов без отправки реальных файлов. Продолжить доступность и адаптивность диалога предложения работы, перевод типов и статусов документов, сценарии заполненного каталога на локальных фикстурах. Общая цель сайта и ЛК остаётся активной. Миграция уязвимых зависимостей SSR/сборки и финальная приёмка всех разделов ещё не завершены.
