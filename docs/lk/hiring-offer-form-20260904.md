# Предложение подрядчику — 4 сентября 2026

## Изменения

Все поля диалога получили связанные подписи и уникальные идентификаторы, включая повторяющиеся пакеты работ. Кнопки удаления имеют понятные имена. Сетка сроков, объёмов и бюджета изменена с пяти/шести колонок на одну–три в зависимости от ширины. Панель добавления пакетов переносится на узком экране, иконки добавления и удаления увеличены до 20 px.

Обработчики отправки, права и структура предложения не менялись.

## Проверки

- HiringOfferDialog.test.tsx: тест доступных имён всех полей, уникальных идентификаторов после добавления второго пакета, удаления, отмены без отправки — прошёл.
- ESLint, TypeScript noEmit, staged diff --check прошли.
- Gortex review: замечаний правил нет, MEDIUM. Целевой тест найден get_test_targets, но review не распознаёт его как полное покрытие.
- В тестовой фикстуре используются вымышленные компания и объект. Рабочие предложения не отправлялись.

## Ограничение приёмки

В рабочем тестовом аккаунте каталог пуст. Для визуальной приёмки создан стенд tools/visual-lk, запускаемый только локально: `npx vite --config tools/visual-lk/vite.config.ts`, адрес `/tools/visual-lk/index.html` на 127.0.0.1:4177. Он использует настоящий диалог, локальный список проектов и callback без сети. Рабочий API не вызывается.

В собственной скрытой IAB-вкладке 50 проверены 390×844, 768×1024 и 1440×1000. На телефоне dialog clientWidth=scrollWidth=380; на планшете 758=758, числовые поля 330–347 px. Длинные названия, добавление второго пакета, прокрутка и клавиатурный переход к нижним действиям проверены. Найдено отсутствие расстояния между мобильными кнопками — добавлен gap-2. На локальном стенде заполнено название работ, отправка завершилась тестовым callback без сетевого запроса.

## Предыдущий выпуск

4e0b7614 успешно развёрнут через CI 33835823246. В собственной скрытой IAB-вкладке 49 проверены загрузка профиля, блок документов, связанные подписи, пустой файловый input и доступные действия. Реальные файлы не загружались; отказ и повтор проверены компонентным тестом. Данные компании не изменялись.

## Следующие этапы

Визуальный стенд заполненного каталога и диалога; оставшиеся формы исходящих предложений; русские типы/статусы документов (backend подтверждает pending при загрузке, остальные статусы ещё нужно проверить); завершение оставшихся разделов и согласованная миграция уязвимых зависимостей. Общая цель активна.
